# Отклик - Приложение для взаимодействия с клиентами

## Описание

Отклик - это современное веб-приложение для удобного взаимодействия с клиентами. Проект состоит из двух частей:

1. **Фронтенд** - React-приложение с использованием современных технологий
2. **Бэкенд** - серверная часть на Node.js с Express и Sequelize

## 🛠 Технологии

### Фронтенд
- `React 19` + `Vite`
- `Redux Toolkit` + `React-Redux`
- `React Router v7`
- `Tailwind CSS` с плагинами
- `Framer Motion` для анимаций
- `ESLint` + `Prettier` для линтинга

### Бэкенд
- `Node.js` + `Express`
- `Sequelize ORM` (PostgreSQL)
- `Express Session` + `CORS`
- `Cookie-parser` + `JWT`
- `Sequelize CLI` для миграций

## 🚀 Установка и запуск

### Фронтенд (папка `ot_click`)
```bash
cd ot_click
cd backend
npm install
cd frontend
npm install
npm dev # режим разработки
npm build # сборка для продакшена

🌐 Демо
Приложение доступно по адресу:
https://marketplace-frontend-vannesals-projects.vercel.app

Для локальной разработки используйте http://localhost:5173